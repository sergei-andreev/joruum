name: CI/CD
on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    env:
      NEXT_TELEMETRY_DISABLED: 1
      CI: true
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with: { version: 10 }
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: 'pnpm' }
      - run: pnpm install
      - name: Run checks in parallel
        run: |
          pnpm run lint &
          pnpm run type-check &
          wait
      - run: pnpm run build
      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-files
          path: .next/
          retention-days: 1
      - name: Deploy Staging
        if: github.ref == 'refs/heads/dev'
        run: curl -X POST -d '{}' "${{ secrets.VERCEL_STAGING_HOOK_URL }}"
      - name: Semantic Release
        if: github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: pnpm run release
      - name: Deploy Production
        if: github.ref == 'refs/heads/main'
        run: curl -X POST -d '{}' "${{ secrets.VERCEL_PRODUCTION_HOOK_URL }}"
      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ CI/CD failed on ${{ github.ref_name }}"
          echo "🔗 Check: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
